{
  "installCommand": "npm ci --no-audit --no-fund",
  "buildCommand": "npm run build",
  "outputDirectory": "dist/o-jogo/browser",
  "rewrites": [
    { "source": "/((?!api/|@|.*\\.[a-zA-Z0-9]+$).*)", "destination": "/index.html" }
  ]
}
